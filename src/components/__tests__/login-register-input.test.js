import React from 'react';
import ReactDOM from 'react-dom';
import { Input }  from '../login-register-input';
import {shallow} from 'enzyme';

describe('Login/Register Input', () => {
  it('renders without crashing', function(){
    //shallow(<Input />);
  });
});